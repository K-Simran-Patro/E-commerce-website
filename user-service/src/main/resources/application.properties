spring.application.name=user-service
server.port=8081

# Supabase — replace <project-ref> with your actual Supabase project ID
spring.datasource.url=jdbc:postgresql://db.<project-ref>.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=DvRD382NSQxHTYAi
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.default_schema=user_schema

# Redis (default localhost — update if using a remote instance)
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Kafka (default localhost — update if using a remote broker)
spring.kafka.bootstrap-servers=localhost:9092

# JWT
jwt.secret=your-secret-key-minimum-32-characters-long
jwt.expiration=86400000